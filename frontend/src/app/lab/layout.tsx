import type { Metadata } from "next";
export const metadata: Metadata = { title: "Caso guiado: seguro dotal educativo 20/10", description: "De la promesa contractual a la prima, reserva y validación independiente de un seguro dotal.", alternates: { canonical: "/lab/" } };
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
